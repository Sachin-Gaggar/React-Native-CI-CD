import { SplashScreen } from "expo-router";
import React, { useEffect } from "react";
import { View, StyleSheet, Text } from "react-native";

type Props = {};
const CICD = (props: Props) => {
  return (
    <View style={styles.container}>
      <Text>Sachin</Text>
    </View>
  );
};
export default CICD;
const styles = StyleSheet.create({
  container: {},
});
